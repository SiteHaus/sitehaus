# TYPE  DATABASE  USER  ADDRESS      METHOD
# Unix socket — trusted (used by CLI psql_capture and local tooling)
local   all       all                trust
# TCP — password required for all connections (Docker network, tunnels)
host    all       all   0.0.0.0/0    scram-sha-256
host    all       all   ::/0         scram-sha-256
