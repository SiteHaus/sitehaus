name: Discord Notify

on: [push]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Discord notification
        uses: Ilshidur/discord-webhook@v1
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK }}
          message: "User ${{ github.actor }} pushed to ${{ github.ref_name }}!"
